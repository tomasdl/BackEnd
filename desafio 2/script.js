 // a. Realizar una función que reciba como parámetro el array y devuelva el 
        // elemento de texto con más caracteres.
        // b. Realizar una función que reciba como parámetro el array y devuelva en qué 
        // posición del array se encuentra un 'false'.
        // c. Crear otra función que devuelva el resultado de la operación entre los dos 
        // elementos numéricos que contiene el array. Dicha función recibirá dos 
        // parámetros: el array y la operación ('suma', 'resta', 'mult', 'div') a realizar.

        
        var valores = [true, 5, false, "hola", "adios", 2];

        const longest = (a) => {
            if(a.map(elem => typeof elem === "string")){
            return console.log("hola")

            }}
            longest(valores);